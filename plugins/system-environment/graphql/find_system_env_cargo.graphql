query findSystemEnvCargo {
  instances {
    entities(label: "/io/reactive-graph/system/env/cargo") {
      id
      label
      type {
        namespace
        name
      }
      properties(names: [
        "label"
        "name"
        "value"
      ]) {
        name
        value
      }
    }
  }
}
