query findSystemEnvDisplay {
  instances {
    entities(label: "/io/reactive-graph/system/env/display") {
      id
      label
      type {
        namespace
        name
      }
      properties(names: [
        "label"
        "name"
        "value"
      ]) {
        name
        value
      }
    }
  }
}
