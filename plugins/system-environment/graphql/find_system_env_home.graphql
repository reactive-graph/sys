query findSystemEnvHome {
  instances {
    entities(label: "/io/reactive-graph/system/env/home") {
      id
      label
      type {
        namespace
        name
      }
      properties(names: [
        "name"
        "value"
      ]) {
        name
        value
      }
    }
  }
}
