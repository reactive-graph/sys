query findSystemEnvPath {
  instances {
    entities(label: "/io/reactive-graph/system/env/path") {
      id
      label
      type {
        namespace
        name
      }
      properties(names: [
        "label"
        "name"
        "value"
      ]) {
        name
        value
      }
    }
  }
}
