query findSystemEnvUsername {
  instances {
    entities(label: "/io/reactive-graph/system/env/username") {
      id
      label
      type {
        namespace
        name
      }
      properties(names: [
        "label"
        "name"
        "value"
      ]) {
        name
        value
      }
    }
  }
}
